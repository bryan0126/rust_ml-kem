[package]
name = "harness_mlkem"
version = "0.1.0"
edition = "2021"

# impl #3: 오픈소스 순수 Rust ML-KEM (RustCrypto ml-kem)
# hazmat: encapsulate_deterministic 등 결정적(KAT용) API 를 정식으로 사용하기 위함
[dependencies]
ml-kem = { version = "0.3", features = ["hazmat"] }
rand = "0.8"
bench_util = { path = "../../../benches/ml-kem/bench_util" }
